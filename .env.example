# Servidor (obrigatório para server.js)
PORT=3000
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_PUBLISHABLE_KEY=sua_chave_anon_publica_do_supabase

# Admin: email que também terá acesso ao painel /admin (opcional; senão, use role 'admin' na tabela users)
# ADMIN_EMAIL=seu@email.com

# Stats do painel admin e backup: chave SECRETA (service_role). Nunca use no frontend.
# O server.js usa esta chave só para /admin/api/stats (contagem real de usuários/sessões).
# backup-db.js e import-questions.js também usam. Pode ser qualquer um dos nomes abaixo:
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_aqui
# SUPABASE_SECRET_KEY=sua_service_role_key_aqui

# CORS (produção): origens permitidas separadas por vírgula. Se não definir, aceita localhost e *.supabase.co.
# ALLOWED_ORIGINS=https://seusite.com,https://www.seusite.com

# Segurança: não commite .env (já no .gitignore). Não duplique SUPABASE_URL no .env.
