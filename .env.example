# Servidor (obrigatório para server.js)
PORT=3000
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_PUBLISHABLE_KEY=sua_chave_anon_publica_do_supabase

# Admin: email que sempre tem acesso ao painel /admin (opcional; padrão: anacssanamum@gmail.com)
# ADMIN_EMAIL=seu@email.com

# Script de importação de questões (import-questions.js)
# Use a Service Role Key do Supabase (Dashboard → Settings → API), nunca no frontend.
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_aqui

# Segurança: não commite .env nem client_secret*.json (já no .gitignore).
# Se client_secret foi commitado antes, execute: git rm --cached 'client_secret*.json'
